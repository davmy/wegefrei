# Copy this file to keystore.properties (which is gitignored) and fill in your real release
# keystore details. Without keystore.properties present, `assembleRelease`/`bundleRelease`
# produce an unsigned build.
storeFile=/absolute/path/to/your/release.keystore
storePassword=changeme
keyAlias=release
keyPassword=changeme
