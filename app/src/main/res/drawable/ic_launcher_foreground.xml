<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Minimalist "Absolutes Halteverbot" (no-stopping) sign: a hollow ring — the center is
         transparent so the background shows through — with a diagonal bar sized to the
         circle: its corners sit exactly on the outer boundary, never extending past it.
         Fill color follows the system's Material You neutral palette (see colors.xml /
         values-night/colors.xml), so it stays dark-on-light or light-on-dark to match
         ic_launcher_background in both the default icon and the OS's light/dark mode. -->
    <path
        android:fillColor="@color/ic_launcher_foreground_color"
        android:fillType="evenOdd"
        android:pathData="M28,54 A26,26 0 1,0 80,54 A26,26 0 1,0 28,54 Z M34,54 A20,20 0 1,0 74,54 A20,20 0 1,0 34,54 Z" />
    <path
        android:fillColor="@color/ic_launcher_foreground_color"
        android:pathData="M69.34,75.00 L75.00,69.34 L38.66,33.00 L33.00,38.66 Z" />
</vector>
